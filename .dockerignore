# Что не должно попасть в образ.
#
# На хостинге код приходит из гита, поэтому лишнего там и так нет. Файл
# нужен для сборки с локальной машины: рядом лежат исходники макета и,
# главное, файл с токеном.

# Секреты
.env
.env.*
!.env.example
*.token

# Исходные файлы макета — сайту не нужны, а весят десятки мегабайт
5260243578280*.jpg
preview/
.impeccable/

# Служебное
.git/
.github/
.claude/
__pycache__/
*.py[cod]
*.log
.DS_Store
Thumbs.db
